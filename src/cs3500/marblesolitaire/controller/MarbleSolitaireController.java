package cs3500.marblesolitaire.controller;

/**
 * The MarbleSolitaireController interface which contains the playGame method and takes in user.
 * Inputs to control the game, sends these inputs back to the model which transmits to the view.
 */
public interface MarbleSolitaireController {

  /**
   * method used to play the game.
   */
  void playGame();
}
